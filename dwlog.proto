syntax = "proto3";

package dwlog;

service LogService {
    rpc GetMessage(Message) returns (Response) {}
}

message Message {
    string app = 1;
    string host = 2;
    string time = 3;
    uint32 level = 4;
    string message = 5;
}

message Response {
    bool result = 1;
}